extends ../index.pug

block variables
    - var template = 'video-scrub'

block content
    .video-scrub-wrapper
        canvas.image__sequence
        //- video.video-scrub(src='videos/MonacoF1.mp4' playsinline='true' webkit-playsinline='true' preload='metadata' muted='muted')